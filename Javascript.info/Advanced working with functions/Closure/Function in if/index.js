
let phrase = "Hello";

if (true) {
  let user = "John";

  function sayHi() {
    console.log(`${phrase}, ${user}`);
  }
}

sayHi();

// Look at the code. What will be the result of the call at the last line?
// Il risultato sarà "Hello, John";